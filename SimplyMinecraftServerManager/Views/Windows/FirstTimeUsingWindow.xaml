<!-- Copyright (c) 2026 We Are Starlight Chaser Team -->
<!-- Licensed under the MIT License. -->
    
<ui:FluentWindow x:Class="SimplyMinecraftServerManager.Views.Windows.FirstTimeUsingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:gif="http://wpfanimatedgif.codeplex.com"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:SimplyMinecraftServerManager.Views.Windows"
        mc:Ignorable="d"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round"
        WindowStartupLocation="CenterScreen"
        ExtendsContentIntoTitleBar="True"
        d:DataContext="{d:DesignInstance local:FirstTimeUsingWindow,IsDesignTimeCreatable=False}"
        Title="{Binding ViewModel.ApplicationTitle,Mode=OneWay}" Height="450" Width="800" WindowStyle="None">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ui:TitleBar Title="{Binding ViewModel.ApplicationTitle,Mode=OneWay}" Grid.Row="0" CloseWindowByDoubleClickOnIcon="True">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="pack://application:,,,/Assets/Pictures/SMSM-icon-256.jpg" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <ContentControl Grid.Row="0" Content="{Binding ViewModel.SelectedTip}">
                <ContentControl.ContentTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TextBlock FontSize="20" FontWeight="Black" Margin="5" 
                           Text="{Binding Title}" HorizontalAlignment="Center"/>
                            <TextBlock FontSize="16" FontWeight="Medium" Margin="5" Grid.Row="1" 
                           Text="{Binding Description}" HorizontalAlignment="Center"/>
                            <ItemsControl Grid.Row="2" ItemsSource="{Binding Tips}" 
                              HorizontalAlignment="Center" VerticalAlignment="Center">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="10,0,10,5">
                                            <Image gif:ImageBehavior.AnimatedSource="{Binding Gif}" Margin="0,0,0,5"/>
                                            <TextBlock Width="125" Text="{Binding Feature}" TextWrapping="WrapWithOverflow"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </DataTemplate>
                </ContentControl.ContentTemplate>
            </ContentControl>
            <ProgressBar Grid.Row="1" Maximum="{Binding ViewModel.Tips.Count}" Value="{Binding ViewModel.TipIndex}" HorizontalAlignment="Center" Width="150"/>
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Margin="5" IsEnabled="{Binding ViewModel.IsBackButtonAvailable}" Command="{Binding ViewModel.PrevTipCommand}">
                    <StackPanel Orientation="Horizontal">
                        <ui:SymbolIcon Symbol="ArrowLeft24" Margin="0,0,5,0"/>
                        <TextBlock>上一步</TextBlock>
                    </StackPanel>
                </Button>
                <ui:Button Appearance="Primary" Margin="5" Visibility="{Binding ViewModel.IsNextButtonAvailable,Converter={StaticResource BooleanToVisibilityConverter}}" Command="{Binding ViewModel.NextTipCommand}">
                    <StackPanel Orientation="Horizontal">
                        <ui:SymbolIcon Symbol="ArrowRight24" Margin="0,0,5,0"/>
                        <TextBlock>下一步</TextBlock>
                    </StackPanel>
                </ui:Button>
                <Button Margin="5" Visibility="{Binding ViewModel.IsNextButtonAvailable,Converter={StaticResource InverseBooleanToVisibilityConverter}}" Command="{Binding ViewModel.FinishTipCommand}">
                    <StackPanel Orientation="Horizontal">
                        <ui:SymbolIcon Symbol="Checkmark24" Margin="0,0,5,0"/>
                        <TextBlock>完成</TextBlock>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</ui:FluentWindow>
