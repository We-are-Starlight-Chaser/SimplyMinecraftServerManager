<Page
  x:Class="SimplyMinecraftServerManager.Views.Pages.DashboardPage"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:local="clr-namespace:SimplyMinecraftServerManager.Views.Pages"
  xmlns:controls="clr-namespace:SimplyMinecraftServerManager.Controls"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
  Title="DashboardPage"
  d:DataContext="{d:DesignInstance local:DashboardPage,
                                     IsDesignTimeCreatable=False}"
  d:DesignHeight="450"
  d:DesignWidth="800"
  ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
  ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
  mc:Ignorable="d"
>
    <Grid VerticalAlignment="Top">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Margin="0,10,0,0" Orientation="Horizontal" Grid.Row="0">
            <ui:Button Command="{Binding ViewModel.NavivateToSiteCommand}" CommandParameter="https://wasc.qzz.io/products/smsm">产品主页</ui:Button>
            <ui:Button Command="{Binding ViewModel.NavivateToSiteCommand}" CommandParameter="https://wasc.qzz.io/tutorials/smsm" Margin="5,0,0,0">使用教程</ui:Button>
        </StackPanel>
        <TextBlock Grid.Row="1" FontSize="18" Margin="0,5,0,0">服务器管理</TextBlock>
        <WrapPanel Grid.Row="2" x:Name="grid_cards" Margin="0,5,0,0">
        </WrapPanel>
    </Grid>
</Page>
